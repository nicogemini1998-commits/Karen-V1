# Firewall — karen-secrets-rotation (T4 PRIVILEGED)
# DENY-ALL por defecto: primer match gana, el catch-all final bloquea todo lo no listado.
# Solo paths mínimos para rotación + audit. Cada Edit fuera de esto = /karen-grant + confirm humano.

ALLOW /.env
ALLOW /.env.local
ALLOW /01-MEMORIA/audit/

DENY  /
